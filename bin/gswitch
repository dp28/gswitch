#!/usr/bin/env ruby
require_relative "../lib/gswitch"

begin
  GSwitch::Application.new(ARGV).run
rescue GSwitch::NotGitRepoError
  abort 
rescue GSwitch::PersistentStack::StackEmptyError
  abort "gswitch: No branch history - staying on current branch."
rescue OptionParser::InvalidOption => err
  abort "gswitch: #{err.message}\nusage: gswitch [-ps] [branch]"
end